# Usage Guide

**One-stop guide to running every tool in `production-tools/`**

---

## Quick Start

```bash
# Clone & enter
git clone https://github.com/yourorg/production-tools.git
cd production-tools

# Install Python deps
pip install -r monitoring/requirements.txt

# Make scripts executable
chmod +x deployment/deploy.sh